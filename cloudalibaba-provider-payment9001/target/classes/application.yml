server:
  port: 9001
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #指定nacos的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'    #暴露所有端点
